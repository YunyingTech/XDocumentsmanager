// SMB utilities — Phase 2
// Handles UNC path normalization, SMB credential management, and authentication.
// For Phase 1, UNC paths work transparently through std::fs when the share
// is already authenticated via Windows credentials.
