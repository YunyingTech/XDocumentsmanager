Platform-specific Python bootstrap archives are downloaded here by prepare-paddle-runtime.mjs.
CPU and CUDA 12.6 Paddle packages are installed on first use into separate application-data runtimes.
RapidOCR uses the same verified Python bootstrap plus a bundled offline wheelhouse and installs into a separate DirectML/CoreML application-data runtime.
